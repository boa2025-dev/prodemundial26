name: Fix Round of 32 bracket teams

on:
  workflow_dispatch:
    inputs:
      dryRun:
        description: 'Dry run (preview only, no writes)'
        type: boolean
        default: true

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Call fix endpoint
        run: |
          if [ "${{ inputs.dryRun }}" = "true" ]; then
            curl --fail --silent --show-error -L \
              -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
              "https://www.prodemundial26.online/api/cron/fix-r32-bracket?dryRun=1"
          else
            curl --fail --silent --show-error -L \
              -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
              "https://www.prodemundial26.online/api/cron/fix-r32-bracket"
          fi
